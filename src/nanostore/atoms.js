import { atom } from "nanostores"
import { persistentAtom } from '@nanostores/persistent'


export const ruta = atom("dashboard")
export const idUsuario = atom("")
export const token = atom("")
export const rol = atom("")